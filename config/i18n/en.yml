# Jobfaro — American English display strings (canonical).
# Every user-facing literal lives here, never hardcoded in code (Phase 1.3).
# config/i18n/es.yml must mirror this key set exactly (enforced by test-all.mjs).

cli:
  tagline: "Bilingual US job-search command center — built for new grads and first-time job-seekers."
  usage: "Usage: jobfaro <command> [options]"
  commands_header: "Commands"
  options_header: "Options"
  command_desc:
    init: "Interactive setup wizard (region, level, profile, language)"
    import: "Upload a résumé → parse it locally + prefill your profile (DOCX/PDF/TXT)"
    scan: "Scan configured career portals for new roles (no model needed)"
    seed: "Materialize portals from the region employer catalog"
    prescreen: "Gate + rank pending roles by likelihood before any eval (no model needed)"
    eval: "Evaluate a role against your résumé — fit score + report"
    recheck: "Re-verify that scored listings are still posted (no model needed)"
    feedback: "Rate a verdict 👍/👎 — builds the local set that calibrates the evaluator"
    report: "Write the beta report (funnel + ratings + agreement, PII-free) to data/reports/"
    pipeline: "Run scan, evaluate, and track end to end"
    pdf: "Render a tailored ATS résumé → output/ (HTML/PDF)"
    outreach: "Find people to contact about a role + keep follow-ups polite (drafts stay yours to send)"
    tracker: "View and update your application tracker"
    dashboard: "Launch the pipeline dashboard (web)"
    tui: "Interactive terminal dashboard"
    serve: "Run the engine API on localhost (web/mobile front-ends plug in here)"
    backend: "Set up / check the on-device inference backend (winc.cpp)"
    calibrate: "Calibrate the evaluator against a hand-banded set (live, opt-in)"
    doctor: "Check that your setup is ready"
    update: "Update Jobfaro to the latest version"
  opt_help: "Show help"
  opt_version: "Show version"
  opt_lang: "Language for this run (en | es)"
  more_help: "Run `jobfaro <command> --help` for details on a command."
  planned: "`jobfaro {command}` isn't built yet — it's planned for {phase}."
  unknown: "Unknown command: {command}. Run `jobfaro --help` to see what's available."

scan:
  title: "Jobfaro scan"
  dry_run_title: "Jobfaro scan — dry run"
  ctx: "Region: {region}  ·  Level(s): {levels}  ·  Language: {language}"
  portals_count: "Portals configured: {count}"
  none: "No portals configured yet. Add entries to config/portals.yml, or run `jobfaro init`."
  would_scan: "would scan"
  no_provider: "no matching provider (check careers_url)"
  dry_note: "Dry run — no network requests were made."
  scanning: "Scanning {count} portal(s)…"
  found_for: "{company}: {count} role(s)"
  error_for: "{company}: {error}"
  total_found: "{count} role(s) found across {portals} portal(s)."
  tally_roles: "{count} on the radar"
  level_note: "{count} role(s) outside your level(s) were filtered out"
  region_note: "{count} role(s) outside your region(s) were filtered out"
  eval_hint: "Discovered roles saved to your pipeline. Scan only finds roles — run `jobfaro eval <url>` to score fit (the model scores, not the scanner)."
  pruned: "{count} stale role(s) pruned (no longer listed on any board)"
  listings_gone: "{count} scored role(s) are no longer on their board — marked \"no longer posted\" (kept, never deleted)"

doctor:
  title: "Jobfaro doctor"
  node_ok: "Node.js {version}"
  node_old: "Node.js {version} — Jobfaro needs >= {min}"
  deps_ok: "Dependencies installed"
  deps_missing: "Dependencies missing — run `npm install`"
  home: "User data home: {home}  (set JOBFARO_HOME to relocate; copy that folder to move devices)"
  config_ok: "Config found (profile.yml + portals.yml)"
  config_missing: "{file} not found — run `jobfaro init` to create it"
  i18n_ok: "Language tables present: {langs}"
  i18n_missing: "Language table missing: {lang}"
  providers_ok: "Scanner providers loaded: {providers}"
  pdf_optional: "PDF export not set up (optional — only needed for `jobfaro pdf`)"
  playwright_optional: "Playwright not installed (optional — only some JS-heavy iCIMS sites need it)"
  link_ok: "Global command(s) on PATH point to this checkout ({cmds})"
  link_missing: "Global command(s) not on PATH ({cmds}) — optional; run `npm link` in this folder to add them"
  link_broken: "`{cmd}` on PATH is a broken link ({target}) — this folder moved or was renamed; run `scripts/after-move.sh` (or `npm rm -g jobfaro jobdar`, then `npm link` here)"
  link_elsewhere: "`{cmd}` on PATH points at a different install ({target}) — if this checkout should own it, re-run `npm link` here"
  resume_import_ok: "Résumé import tools present (unzip for .docx, pdftotext for .pdf)"
  pdftotext_optional: "pdftotext (poppler) not found — PDF résumé import/upload needs it: `brew install poppler` or `apt-get install poppler-utils` (.docx still works)"
  unzip_optional: "unzip not found — .docx résumé import needs it (install via your package manager)"
  summary_ok: "All good — you're ready to scan."
  summary_warn: "{count} optional item(s) not set up. You can still scan, evaluate, and track."
  summary_fail: "{count} problem(s) found. Fix the items marked with an x above, then re-run `jobfaro doctor`."

tracker:
  title: "Application tracker"
  empty: "No applications tracked yet — press `a` on a role in `jobfaro tui`, or run `jobfaro tracker --set <url> applied`."
  count: "{count} application(s) tracked"
  set_usage: "Usage: jobfaro tracker --set <url> <state>   (states: {states})"
  set_missing: "No pipeline role with that URL — run `jobfaro scan` first ({url})"
  set_done: "Marked {state} — {url}"
  col_company: "Company"
  col_role: "Role"
  col_state: "State"
  col_updated: "Updated"

regions:
  midwest: "Midwest"
  northeast: "Northeast"
  southeast: "Southeast"
  southwest: "Southwest"
  west: "West"
  nationwide: "Nationwide"
  custom: "Custom"

levels:
  entry: "entry"
  mid: "mid"
  senior: "senior"

seed:
  title: "Region employer seeds"
  matched: "{count} employer(s) for: {regions}"
  none: "No seeded employers for that selection yet."
  written: "Wrote {count} employer(s) to {file}"
  hint: "Re-run with --write to save these to config/portals.yml"

init:
  title: "Jobfaro setup"
  intro: "A few quick questions — then Jobfaro writes your config and you can scan. Press Enter for a default."
  q_language: "Language / Idioma:"
  q_name: "Your name:"
  q_location: "Your metro (e.g. Indianapolis, IN):"
  q_salary: "Target annual salary in USD (or blank to skip):"
  q_region: "Which region should Jobfaro focus on?"
  q_levels: "Which level of roles?"
  lvl_entry: "Entry-level only (default)"
  lvl_entry_mid: "Entry + mid-level"
  lvl_all: "Entry + mid + senior"
  q_tuning: "Which best describes you?"
  tun_new_grad: "New grad (default)"
  tun_early_career: "Early career (1–3 years)"
  tun_no_degree: "No degree / skills-based"
  tun_career_changer: "Career changer"
  q_transferable: "Include roles that fit your transferable skills (good for career changers)?"
  xfer_yes: "Yes — surface strong cross-field fits (still selective)"
  xfer_no: "No — only direct-field matches"
  q_inference: "How should Jobfaro evaluate roles?"
  inf_api: "My own API key (cloud, higher quality)"
  inf_local: "On-device model — private, no key, no cost (default)"
  local_hint: "On-device backend selected — set it up with: jobfaro backend --install"
  q_api_key: "Paste your API key (or leave blank — model-backed commands use your AI CLI today):"
  key_saved: "Saved your key to {file} (gitignored). It's the api backend for eval/tailoring — or use the on-device default (jobfaro backend)."
  key_skipped: "No key stored — that's fine. Eval/tailoring run via your AI CLI; add a key later if you want."
  done: "Wrote {profile} and seeded {portals} employer(s) into config/portals.yml."
  seeded_none: "Wrote {profile}. No seeded employers for that region yet — add some to config/portals.yml or try --region nationwide."
  next: "Next: run `jobfaro scan` to see roles, then `jobfaro eval <url>` to score one."
  resume_saved: "Saved your résumé to {file} and prefilled your profile."
  resume_manual: "{file} looks like a {kind} — open it in your AI CLI (the onboard mode reads it) to build cv.md."
import:
  title: "Import a résumé"
  usage: "Usage: jobfaro import <file.docx|file.pdf|file.txt> [--write]"
  extracted: "Read {chars} characters from your {kind}."
  summary: "Profile Jobfaro will write:"
  f_name: "Name"
  f_location: "Metro"
  f_level: "Level"
  f_skills: "Skills"
  f_cv: "Résumé"
  chars: "characters"
  backend: "structured by: {backend}"
  heuristic: "heuristic (no model)"
  dry: "Preview only — re-run with --write to save config/profile.yml + data/cv.md."
  saved: "Saved {profile} + {cv}. Run `jobfaro scan` next."
  err_not-found: "File not found: {file}"
  err_empty: "Couldn't read any text from {file} — is it empty?"
  err_pdf-no-extractor: "PDF extraction needs pdftotext (poppler) here — or export the résumé as DOCX/TXT."
  err_image-pdf: "{file} looks like a scanned/image PDF (no text layer) — export it as text or DOCX."
  err_unsupported: "Unsupported file type: {file}. Use DOCX, PDF, TXT, or MD."

serve:
  title: "Jobfaro engine server"
  up: "Engine API on {url} — POST /evaluate, /import; GET /health."
  gui: "GUI on {url} (serving {dir})"
  endpoints: "The web/mobile front-ends talk to this one socket (localhost only)."
  stop: "Press Ctrl-C to stop."


dashboard:
  title: "Jobfaro dashboard"
  region: "Region"
  level: "Level(s)"
  language: "Language"
  tracker: "Application tracker"
  portals: "Portals"
  serving: "Dashboard running at {url} — open it in your browser."
  stop: "Press Ctrl-C to stop."
  footer: "Your data stays local. Jobfaro reads only public job listings."
  access: "Open your dashboard — `jobfaro tui` (terminal) or `jobfaro dashboard` (web · http://localhost:4319)"
  analytics: "Analytics"
  pipeline: "Pipeline"
  all: "All"
  no_eval: "No evaluated roles yet — run `jobfaro eval <url>` to score some."
  stat_summary: "{evaluated} of {total} roles evaluated · avg fit {avg}"
  chart_bands: "By band"
  chart_scores: "Score distribution"
  chart_companies: "Top companies"
  chart_sectors: "By sector"
  chart_locations: "By location"
  chart_funnel: "Pipeline funnel"
  funnel_discovered: "Discovered"
  funnel_evaluated: "Evaluated"
  funnel_applytier: "Apply-tier"
  funnel_applied: "Applied"

tui:
  keys: "↑/↓ move · ⏎/o open · a applied · PgUp/PgDn g/G jump · s sort · 1/2/3 band · p pending · 0 all · c company · r refresh · q quit"
  gone: "gone ✗"
  empty: "No roles yet — run `jobfaro scan` to discover roles, then `jobfaro eval <url>` to score them."
  none_match: "No roles match this filter."
  pending: "pending"
  position: "{from}–{to} of {total}"
  col_score: "Score"
  col_band: "Band"
  col_role: "Role"
  col_company: "Company"
  col_location: "Location"
  by_score: "score"
  by_company: "company"
  by_band: "band"
  sorted: "sorted by {mode}"

bands:
  apply: "Apply"
  research: "Research"
  dont: "Don't"

eval:
  title: "Evaluate a role"
  next_for: "Next pending: {role} @ {company} ({remaining} pending)"
  next_none: "Nothing pending — every discovered role is evaluated. Run `jobfaro scan` for fresh roles."
  next_screened_hint: "{count} role(s) are screened out by prescreen — re-run with --include-screened to see them anyway."
  next_screened_note: "prescreen screened this role — {reason}"
  jd_for: "Job description — {role} (fetched for you; score it against your résumé):"
  today: "Today's date is {date} — treat any “Present”/“Current” on the résumé as this date, never as a future date."
  jd_none: "Couldn't fetch the JD automatically — open {url} to read it, then score."
  jd_error: "Couldn't fetch the JD ({error}) — open the URL to read it, then score."
  model_needed: "Evaluation is model-backed — run `/jobfaro eval <url>` in your AI CLI. It reads modes/eval.md, scores fit against your résumé, and records the verdict."
  save_hint: "It saves with: jobfaro eval --save --url <url> --score <0.0–5.0> --band <apply|research|dont> --company \"…\" --role \"…\" --note \"…\""
  save_usage: "Usage: jobfaro eval --save --url <url> --score <0.0–5.0> [--band apply|research|dont] [--company …] [--role …] [--note …]"
  saved: "Saved fit {score} ({band}) — {role}"
  band_mismatch: "Refusing to save: band \"{band}\" contradicts score {score} (which derives to \"{derived}\"). The band always follows the score — drop --band or fix the score."
  no_cv: "No résumé on file — refusing to score. Without a résumé the model invents one and returns a confident but meaningless verdict."
  no_cv_hint: "Add your résumé (jobfaro import <file>, or save it as data/cv.md), then re-run."
  auto_backend_down: "No inference backend is ready ({reason})."
  auto_running: "Auto-evaluating {count} role(s) via the {backend} backend…"
  auto_no_jd: "skipped (couldn't fetch the JD): {role}"
  auto_unparsed: "skipped (model reply didn't parse): {role}"
  auto_error: "failed ({error}): {role}"
  auto_clamped: "clamped"
  auto_done: "Auto-eval done — {evaluated} scored, {failed} skipped."
  batch_skipped: "--batch needs the api backend and more than one role — running live instead."
  preconfirm_skip: "pre-confirm skipped {role} — {reason}"
  preconfirm_thinned: "{skipped} role(s) skipped by AI pre-confirm before the scoring pass."
  escalated: "escalated borderline {from} → {to} on the api backend"
  next_capped: "Capping this run at {max} roles (you asked for {n}) — run it again for the rest."
  report_where: "Your jobs report: {file}"
  report_view: "View it: jobfaro tracker (table) · jobfaro tui (interactive) · jobfaro dashboard (web)"
  report_more: "Evaluate more ({pending} pending): jobfaro eval --next 5 · 10 · 15 — any number up to 50"
  report_scope: "Scores judge the listing text against your résumé — the employer itself isn't verified."
  dist_warn: "{applyCount} of {evaluated} verdicts landed in Apply ({pct}%) — unusually high. Rate a few (👍/👎 in the app) and run `jobfaro calibrate --feedback` to see if the evaluator is running hot."
  batch_waiting: "Batches job submitted — sweeping until the results land (the 50%-price tier takes its time)…"

feedback:
  title: "Rate a verdict"
  usage: "Usage: jobfaro feedback <url | company-or-role text> --good | --bad"
  saved: "Recorded on {role} @ {company} (scored {score} {band})"
  none: "No evaluated role matches \"{target}\" — feedback labels a verdict, so the role must be scored first."
  none_hint: "See scored roles: jobfaro tracker · score one: jobfaro eval --next"
  ambiguous: "{count} scored roles match \"{target}\" — narrow it, or pass the exact --url:"
  tally: "{n} label(s) so far — the evaluator agrees with you {agreement}% of the time."
  calibrate_hint: "See where it's wrong: jobfaro calibrate --feedback"
  more_hint: "{need} more label(s) and `jobfaro calibrate --feedback` starts telling you something real."

report:
  title: "Beta report"
  written: "Report written: {file}"
  summary: "{evaluated} roles scored · {rated} rated by you · evaluator agreement {agreement}"
  no_ratings: "No roles rated yet — answer \"Would you apply?\" on scored roles (app thumbs or `jobfaro feedback`), then re-run."
  privacy: "The report contains no résumé content and no personal data beyond region/level settings — safe to share."

recheck:
  title: "Re-check listings"
  none: "Nothing to re-check — no evaluated Apply/Research roles in the pipeline (use --all for every scored role, --url for one)."
  running: "Re-checking {count} listing(s) against their boards…"
  tally_live: "still posted"
  tally_gone: "gone"
  gone_line: "no longer posted: {company} — {role}"
  done: "Re-check done — {live} still posted, {gone} no longer posted, {unknown} unreachable."
  gone_note: "Gone roles keep their score and history, but leave the eval queue and show \"no longer posted\" — nothing is deleted."
  unknown_note: "{count} board(s) couldn't be reached — those rows are unchanged (an unreachable board proves nothing)."

backend:
  title: "Inference backend"
  mode: "Mode: {mode}  (local = on-device · api = your key · auto = local then api)"
  local_url: "Local ({runtime}): {url}"
  api_url: "Anthropic API: {url}  (model {model})"
  up: "● up — serving /v1/messages"
  down: "○ down — not serving"
  key_set: "● API key found (data/credentials.env)"
  key_missing: "○ no API key — run `jobfaro init` or set JOBFARO_API_KEY"
  reason: "{reason}"
  start_hint: "Start the local model:  winc serve --eval"
  start_hint_other: "Start your {runtime} server (it must serve the OpenAI chat API)."
  install_hint: "First time?  jobfaro backend --install"
  check_hint: "Verify end to end:  jobfaro backend --check"
  checking: "Probing {kind} backend at {url} …"
  health_ok: "● /health OK"
  health_fail: "○ /health failed — the local server isn't up"
  canary_ok: "● round-trip OK — eval scored {score} ({band}) via {model} · {tokens} tokens"
  canary_unparsed: "○ backend replied but the verdict didn't parse: {raw}"
  empty_hint: "The server is up but replied empty — start it with the eval profile: winc serve --eval (not bare `winc serve`)."
  empty_hint_other: "The model replied off-format — try a stronger local model (set local_model) or the api backend."
  model_hint: "The model isn't available — set local_model in profile.yml and pull it (e.g. ollama pull <model>)."
  canary_fail: "○ round-trip failed: {error}"
  no_winc: "winc.cpp is not installed."
  install_prebuilt: "Prebuilt (no compiler): download a winc-jobdar release, then run `winc setup`"
  install_source: "From source: git clone -b winc-jobdar https://github.com/samdotson61/winc.cpp && cd winc.cpp && ./install.sh"
  install_after: "Then re-run: jobfaro backend --install"
  found_winc: "Found winc {version}."
  setup_step: "Ensure a model:  winc setup   (auto-picks a tier; downloads if needed)"
  serve_step: "Start the eval server:  winc serve --eval"
  already_up: "● already serving at {url}"
  serve_then_check: "After `winc serve --eval`, verify with: jobfaro backend --check  ({url})"

calibrate:
  title: "Calibrate the evaluator"
  none: "No calibration set at {file}. Provide a JSON array of hand-banded roles (each: url or jd, plus expect)."
  starter_note: "Using the bundled starter set (synthetic smoke check). Your own labeled set at {file} takes over once it exists — build it from real verdicts with `jobfaro feedback`."
  backend_down: "No inference backend is ready ({reason})."
  running: "Scoring {count} calibration role(s) via the {backend} backend…"
  result: "Band agreement: {overall}% over {n} roles."
  tier: "{tier}: {correct}/{total}"
  dist: "Score distribution — Apply {apply} · Research {research} · Don't {dont} (of {n}). A healthy set uses all three bands; an empty Research band means the eval is bimodal."
  fb_none: "No eval feedback yet. Thumbs-rate a few verdicts in the app, then re-run `jobfaro calibrate --feedback`."
  fb_result: "Feedback agreement: {agree}/{n} verdicts you approved ({pct}%). {down} the eval got wrong:"

pdf:
  title: "Build résumé"
  no_cv: "No résumé yet. Add one with `jobfaro init --resume <file>` (or paste it via the onboard mode)."
  html_written: "Wrote {file}"
  pdf_written: "Wrote {file}"
  no_playwright: "HTML written. For an automatic PDF run `npm i playwright`, or open the HTML and Print → Save as PDF."

tailor:
  title: "Tailor — CV summary + cover letter"
  no_cv: "No résumé yet. Add one with `jobfaro init --resume <file>`."
  no_target: "Name a role: `jobfaro tailor <company|url>`, or pass `--jd <jd-file>`."
  no_jd: "Couldn't get the job description for that role."
  backend_down: "Inference backend not ready ({reason}). Start it: `winc serve --eval`."
  running: "Tailoring to {role} on {backend}…"
  failed: "Tailoring failed — the model reply didn't parse. Try again, or a larger local model."
  summary_label: "Tailored summary:"
  written: "Wrote {cv} and {cover}"
  cover_short: "Note: the cover letter came back short — review it before sending."
  pdf_hint: "Render it: copy the tailored CV over data/cv.md, then `jobfaro pdf`."
  directives_applied: "Applying {count} directive(s) at low temperature…"
  directives_list_header: "Directives applied:"
  variant_written: "Wrote {variant}: {cv} and {cover}"
  no_change: "No change — {variant} is already current. Edit the résumé or pass --instruct \"…\" to make a new variant."
  list_header: "Customization for {role}:"
  list_none: "No directives yet — add one with --instruct \"make it warmer\"."
  list_variant: "Current variant: {variant}"

prescreen:
  title: "Prescreen — gate + rank before eval"
  no_cv: "No résumé (cv.md) yet — skill overlap will read neutral. Add one with `jobfaro init --resume <file>`."
  none: "Nothing to prescreen — no pending roles without a prescreen score. Use --rescore to redo all pending."
  checking: "Checking {count} pending role(s) — fetching each JD politely…"
  ranked_header: "Eval queue, most winnable first ({count}):"
  screened_header: "Screened out — each with its reason, nothing hidden ({count}):"
  override_hint: "Still want one? `jobfaro eval --next --include-screened`, or `jobfaro eval <url>` directly."
  done: "Prescreen done: {ranked} ranked, {screened} screened. `jobfaro eval --next` now serves the best role first."
  tally_ranked: "{count} ranked"
  tally_screened: "{count} screened"
  jd_unavailable: "JD unavailable — scored on freshness only"
  reason_years: "asks for more experience than your level"
  reason_clearance: "requires an active security clearance"
  reason_degree: "degree-gated (you excluded degree-required roles)"
  reason_sponsorship: "no visa sponsorship offered (you need sponsorship)"
  flag_clearance: "clearance mentioned"
  flag_degree_stretch: "degree asked — worth a shot anyway"
  flag_sponsorship: "no visa sponsorship"
  flag_license: "license/cert required"
  note_sponsors: "sponsors visa"

outreach:
  title: "Outreach — a warm contact beats a cold application"
  usage: "Usage: jobfaro outreach <url|company> · --log/--followup --url <u> --person \"Name\" · --due · --lint <file|-> · --list"
  for_role: "{role} @ {company}"
  link_recruiters: "Recruiters & talent acquisition (you browse, you choose):"
  link_hiring_manager: "Likely hiring managers:"
  link_company_people: "Everyone at the company:"
  cadence_state: "Contacts for this role: {used} of {max} (the cap keeps it polite)"
  privacy_note: "Privacy: Jobfaro never scrapes LinkedIn or sends messages. Paste a person's public headline into the outreach mode for a tailored draft — the paste is used for that draft and never stored."
  next_steps: "Sent something? Log it: `jobfaro outreach --log --url <roleUrl> --person \"Name\"` — then `jobfaro outreach --due` tells you when the one polite follow-up is ripe."
  log_usage: "Usage: jobfaro outreach --log|--followup --url <roleUrl> --person \"Full Name\" [--title …] [--channel linkedin|email] [--note …]"
  contact_logged: "Logged contact: {person} ({company}). Good luck!"
  contact_duplicate: "You already contacted {person} about this role — one thread per person. `--due` shows when the follow-up is ripe."
  contact_cap: "This role already has {max} contact(s) — more reads as pestering the company. Pick a different role or wait for replies."
  followup_logged: "Logged the follow-up to {person} ({company}). That thread is now closed — no more messages to them about this role."
  followup_reminder: "One polite follow-up is ripe after {days} business days — `jobfaro outreach --due` will tell you."
  followup_stop: "{person} already got the one follow-up — that thread is closed. No exceptions; silence is an answer."
  followup_no_contact: "No first contact logged for {person} on this role — log the contact first with --log."
  followup_too_soon: "Too soon for {person} — {elapsed} of {needed} business days. Patience reads as professionalism (--force if they replied and invited it)."
  due_none: "No follow-ups due — nothing ripe and nothing pending."
  due_line: "{person} ({company}) — {days} business days since contact. One polite follow-up is ripe."
  closed_line: "{person} ({company}) — follow-up sent; thread closed."
  list_empty: "No outreach logged yet — `jobfaro outreach <url>` finds people for a role."
  lint_ok: "Draft passes ({channel}). Read it aloud once, then send it yourself."
  lint_empty: "Draft is empty."
  lint_too_long: "Too long for a LinkedIn note: {length} chars (max {max}). Cut to one hook, one fit, one ask."
  lint_placeholder: "Leftover template placeholder: {quote}"
  lint_missing_name: "The recipient's name ({person}) never appears — a note without their name reads as a blast."
  no_cv: "No résumé yet. Add one with `jobfaro init --resume <file>`."
  draft_no_target: "Name a role: `jobfaro outreach --draft <company|url>` (or `--jd <file>`); add `--person \"Name\"`."
  draft_no_jd: "Couldn't get the job description for that role."
  draft_backend_down: "Inference backend not ready ({reason}). Start it: `winc serve --eval`."
  draft_running: "Drafting outreach for {role} on {backend}…"
  draft_failed: "Draft failed — the model reply didn't parse. Try again, or a larger local model."
  draft_no_change: "No change — {variant} is already current. Change --person/--instruct or edit the résumé for a new variant."
  draft_written: "Wrote {variant}: {file}"
  draft_send_hint: "Review it, send it yourself, then log it: `jobfaro outreach --log --url <roleUrl> --person \"Name\"`."

common:
  none: "none"
  jd_fetch_error: "Couldn't fetch that posting ({error}) — the listing may have closed. Open {url} in a browser to check."
  jd_alternatives: "{count} other matching pipeline role(s) — try one of these:"
